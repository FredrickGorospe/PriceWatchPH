# Intentionally empty for this task: pytest-django reads DJANGO_SETTINGS_MODULE
# from pytest.ini, and no shared fixtures exist yet. Present so later tasks have
# a place to add them without introducing a new root-level file.
